build:
  nodes:
    coverage:
      tests:
        override:
#          - command: ./phpunit --no-coverage
          - command: ./phpunit
            coverage:
              file: clover.xml
              format: clover
